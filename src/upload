#!/usr/bin/env python3

import sys
import os

sys.pycache_prefix = "/tmp/pycache"
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "lib"))

from command.upload import UploadCommand

if __name__ == "__main__":
    UploadCommand().execute()
