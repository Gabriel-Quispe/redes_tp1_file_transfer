# Utilidades

**Agregar Imagenes:**
```md
![Upload](img/upload.png)
```

# Para la Entrega

**Generar PDF:**
```bash
pandoc doc/informe.md -o informe.pdf
```

**Requerimientos:**
```bash
sudo apt install pandoc
sudo apt install texlive-latex-base
sudo apt install texlive-latex-extra
```
