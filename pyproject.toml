[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src/lib"]

[project]
name = "redes-tp1"
version = "0.1.0"
requires-python = ">=3.10"

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-bdd",
]
